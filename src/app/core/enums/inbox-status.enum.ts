export enum InboxStatus {
  pending = 'pending',
  confirmed = 'confirmed',
  archived = 'archived'
}

export enum InboxStatus_Fr {
  pending = 'À Traiter',
  confirmed = 'Résolu',
  archived = 'Archivé'
}
