export enum InboxStatus {
  pending = 'pending',
  to_verify = 'to_verify',
  confirmed = 'confirmed',
  archived = 'archived'
}

export enum InboxStatus_Fr {
  pending = 'À Traiter',
  to_verify = 'À Vérifier',
  confirmed = 'Résolu',
  archived = 'Archivé'
}
